name: Run Clipmaker

on:
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Run clipmaker
        uses: ./.github/actions/clipmaker
